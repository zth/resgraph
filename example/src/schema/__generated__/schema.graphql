scalar Timestamp

scalar Timestamp2

interface HasName {
  name: String!
}

type Query {
  currentTime: Timestamp2
  me(onlyIfAvailable: Boolean!): User
}

type User implements HasName {
  name: String!
  age: Int!
}

